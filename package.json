{
  "name": "classic-men",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "admin",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,admin,server -c cyan,magenta,green \"npm run dev --workspace client\" \"npm run dev --workspace admin\" \"npm run dev --workspace server\"",
    "build": "npm run build --workspace client && npm run build --workspace admin && npm run build --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
